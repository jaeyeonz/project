<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- namespace : mapper 파일을 구분할 수 있는 고유 식별자 -->
  <!-- 이름을 지정해줄 때 DAO 클래스파일명과 일치시키는 경우가 많음 -->
<mapper namespace="cookking2.model.DAO">
	<!-- parameterType : alias 지정했는지 확인할 것 -->
	
	<select id="Login" parameterType="VO" resultType="VO">
		select * from member 
		where log_id = #{userId} and pw = #{pw}
	</select>
	
	<select id="idcheck" parameterType="String" resultType = "Integer">
		select count(*) from member where log_id = #{userId}
	</select>	
	
	<insert id="SignupInsert"	parameterType="VO">
 		insert into member
 		values(#{userId},
 			   #{pw},
 			   #{name},
 			   #{favIndr1}, 
 			   #{favIndr2}, 
 			   #{favIndr3}) 	
	</insert>


</mapper>